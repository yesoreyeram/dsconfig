{
    "$schema": "https://raw.githubusercontent.com/grafana/dsconfig/refs/heads/main/dsconfig/schema.json",
    "schemaVersion": "v1",
    "pluginType": "grafana-netlify-datasource",
    "pluginName": "Netlify",
    "docURL": "https://grafana.com/docs/plugins/grafana-netlify-datasource",
    "fields": [
        {
            "id": "jsonData_services_Netlify_auth_id",
            "key": "id",
            "section": "services.Netlify.auth",
            "label": "Personal access tokens",
            "description": "Netlify REST API Key. Found here: https://app.netlify.com/user/applications#personal-access-tokens",
            "valueType": "string",
            "target": "jsonData",
            "role": "auth.discriminator",
            "defaultValue": "bearer_token",
            "validations": [
                {
                    "type": "allowedValues",
                    "values": [
                        "bearer_token"
                    ]
                }
            ],
            "ui": {
                "component": "select",
                "options": [
                    {
                        "label": "Personal access tokens",
                        "value": "bearer_token"
                    }
                ]
            }
        },
        {
            "id": "secureJsonData_Netlify_token",
            "key": "Netlify.token",
            "label": "Token",
            "description": "Token for accessing the datasource API",
            "valueType": "string",
            "target": "secureJsonData",
            "role": "auth.bearer.token",
            "dependsOn": "jsonData_services_Netlify_auth_id == 'bearer_token'",
            "requiredWhen": "jsonData_services_Netlify_auth_id == 'bearer_token'",
            "ui": {
                "component": "input",
                "placeholder": "Token value"
            }
        }
    ],
    "groups": [
        {
            "id": "authentication",
            "title": "Authentication",
            "order": 1,
            "fieldRefs": [
                "jsonData_services_Netlify_auth_id",
                "secureJsonData_Netlify_token"
            ]
        }
    ],
    "instructions": [
        {
            "msg": "Netlify configuration is stored under a service-keyed jsonData shape: jsonData.services.Netlify.* holds per-service settings (note the service id is capitalized 'Netlify'), while secrets use flat dotted secureJsonData keys of the form \"Netlify.<secret>\". There is a single service with id 'Netlify'.",
            "tags": [
                "llm",
                "structure"
            ]
        },
        {
            "msg": "Authentication uses a Netlify personal access token (bearer). jsonData.services.Netlify.auth.id is 'bearer_token' (the backend defaults to it) and the token is the write-only secret at secureJsonData key \"Netlify.token\".",
            "tags": [
                "llm",
                "auth"
            ]
        },
        {
            "msg": "Minimal working payload: {\"jsonData\":{\"services\":{\"Netlify\":{\"auth\":{\"id\":\"bearer_token\"}}}},\"secureJsonData\":{\"Netlify.token\":\"<netlify-personal-access-token>\"}}. Create a token at https://app.netlify.com/user/applications#personal-access-tokens.",
            "tags": [
                "llm",
                "auth"
            ]
        },
        {
            "msg": "Connection: the server URL is fixed at https://api.netlify.com/api/v1 (no server selector and no connection variables).",
            "tags": [
                "llm",
                "connection"
            ]
        },
        {
            "msg": "The secret at secureJsonData key \"Netlify.token\" is write-only: it can be set or reset but never read back. To check whether it is already configured, read secureJsonFields[\"Netlify.token\"] == true rather than secureJsonData.",
            "tags": [
                "llm",
                "auth",
                "secure"
            ]
        }
    ]
}
