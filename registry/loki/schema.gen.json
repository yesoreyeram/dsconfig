{
  "targetApiVersion": "v0alpha1",
  "settings": {
    "spec": {
      "type": "object",
      "properties": {
        "basicAuth": {
          "type": "boolean",
          "default": false
        },
        "basicAuthUser": {
          "description": "The username of the data source account",
          "type": "string",
          "x-dsconfig-depends-on": "virtual_authMethod == 'BasicAuth'",
          "x-dsconfig-required-when": "root_basicAuth == true"
        },
        "jsonData": {
          "type": "object",
          "properties": {
            "derivedFields": {
              "description": "Derived fields can be used to extract new fields from a log message and create a link from its value.",
              "type": "array",
              "items": {
                "type": "object",
                "required": [
                  "name",
                  "matcherRegex"
                ],
                "properties": {
                  "datasourceUid": {
                    "description": "The tracing data source this internal link should navigate to. Setting this makes the derived field an internal link.",
                    "type": "string"
                  },
                  "matcherRegex": {
                    "description": "Use to parse and capture some part of the log message. You can use the captured groups in the template.",
                    "type": "string"
                  },
                  "matcherType": {
                    "description": "Derived fields can be created from labels or by applying a regular expression to the log message.",
                    "type": "string",
                    "default": "regex",
                    "enum": [
                      "regex",
                      "label"
                    ]
                  },
                  "name": {
                    "type": "string"
                  },
                  "targetBlank": {
                    "type": "boolean"
                  },
                  "url": {
                    "type": "string"
                  },
                  "urlDisplayLabel": {
                    "description": "Use to override the button label when this derived field is found in a log.",
                    "type": "string"
                  }
                }
              }
            },
            "keepCookies": {
              "description": "Grafana proxy deletes forwarded cookies by default. Specify cookies by name that should be forwarded to the data source.",
              "type": "array",
              "items": {
                "type": "string"
              }
            },
            "manageAlerts": {
              "description": "Manage alert rules for this data source. To manage other alerting resources, add an Alertmanager data source.",
              "type": "boolean"
            },
            "maxLines": {
              "description": "Loki queries must contain a limit of the maximum number of lines returned (default: 1000). Increase this limit to have a bigger result set for ad-hoc analysis. Decrease this limit if your browser becomes sluggish when displaying the log results.",
              "type": "string"
            },
            "oauthPassThru": {
              "type": "boolean",
              "default": false
            },
            "serverName": {
              "description": "A Servername is used to verify the hostname on the returned certificate",
              "type": "string",
              "x-dsconfig-depends-on": "jsonData_tlsAuth == true",
              "x-dsconfig-required-when": "jsonData_tlsAuth == true"
            },
            "timeout": {
              "description": "HTTP request timeout in seconds",
              "type": "number"
            },
            "tlsAuth": {
              "description": "Validate using TLS client authentication, in which the server authenticates the client",
              "type": "boolean",
              "default": false
            },
            "tlsAuthWithCACert": {
              "description": "Add your own Certificate Authority (CA) certificate on top of one generated by the certificate authorities for additional security measures",
              "type": "boolean",
              "default": false
            },
            "tlsSkipVerify": {
              "description": "Skipping TLS certificate validation is not recommended unless absolutely necessary or for testing",
              "type": "boolean",
              "default": false
            }
          }
        },
        "url": {
          "type": "string",
          "x-dsconfig-required-when": "true"
        }
      }
    },
    "secureValues": [
      {
        "key": "basicAuthPassword",
        "description": "The password of the data source account"
      },
      {
        "key": "tlsCACert",
        "description": "Your self-signed certificate"
      },
      {
        "key": "tlsClientCert",
        "description": "The client certificate can be generated from a Certificate Authority or be self-signed"
      },
      {
        "key": "tlsClientKey",
        "description": "The client key can be generated from a Certificate Authority or be self-signed"
      }
    ]
  },
  "settingsExamples": {
    "examples": {
      "": {
        "summary": "Default configuration",
        "description": "The defaults a new datasource starts with: no authentication, no TLS overrides, no derived fields. Only root.url needs to be filled in to get a working datasource pointed at a Loki server on localhost:3100.",
        "value": {
          "jsonData": {},
          "secureJsonData": {
            "basicAuthPassword": ""
          },
          "url": "http://localhost:3100"
        }
      },
      "basicAuth": {
        "summary": "Basic authentication",
        "description": "Authenticate with a Loki that requires HTTP Basic. Both basicAuth and basicAuthUser live at the datasource root; only basicAuthPassword is secret.",
        "value": {
          "basicAuth": true,
          "basicAuthUser": "grafana",
          "jsonData": {},
          "secureJsonData": {
            "basicAuthPassword": "REPLACE_WITH_PASSWORD"
          },
          "url": "https://loki.example.com"
        }
      },
      "noAuth": {
        "summary": "No authentication",
        "description": "A public or network-isolated Loki with no HTTP-level auth. Maximum lines is bumped to 5000 for ad-hoc analysis and alerting UI management is enabled.",
        "value": {
          "jsonData": {
            "manageAlerts": true,
            "maxLines": "5000"
          },
          "secureJsonData": {
            "basicAuthPassword": ""
          },
          "url": "http://loki.example.com:3100"
        }
      },
      "oauthForward": {
        "summary": "Forward OAuth Identity",
        "description": "Forward the signed-in user's upstream OAuth identity to Loki. The editor writes only jsonData.oauthPassThru; there is no accompanying secret.",
        "value": {
          "jsonData": {
            "oauthPassThru": true
          },
          "secureJsonData": {
            "basicAuthPassword": ""
          },
          "url": "https://loki.example.com"
        }
      },
      "tlsMutualAuth": {
        "summary": "TLS mutual auth (mTLS)",
        "description": "Loki requiring mTLS. jsonData.tlsAuth=true triggers the SDK to build a client-authenticated transport using serverName plus the PEM-encoded client cert and key in secureJsonData.",
        "value": {
          "jsonData": {
            "serverName": "loki.example.com",
            "tlsAuth": true
          },
          "secureJsonData": {
            "tlsClientCert": "-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----",
            "tlsClientKey": "-----BEGIN RSA PRIVATE KEY-----\n...\n-----END RSA PRIVATE KEY-----"
          },
          "url": "https://loki.example.com"
        }
      },
      "tlsSelfSignedCA": {
        "summary": "Self-signed CA verification",
        "description": "Loki behind a private CA. jsonData.tlsAuthWithCACert=true tells the SDK to verify the server certificate against the PEM-encoded CA in secureJsonData.tlsCACert.",
        "value": {
          "jsonData": {
            "tlsAuthWithCACert": true
          },
          "secureJsonData": {
            "tlsCACert": "-----BEGIN CERTIFICATE-----\n...\n-----END CERTIFICATE-----"
          },
          "url": "https://loki.internal.corp"
        }
      },
      "withDerivedFields": {
        "summary": "Basic auth with derived fields",
        "description": "Loki configured with a derived field that extracts a trace ID from the log line via regex and internally links to a Tempo data source. Two derived fields — one regex-based external link, one label-based internal link — demonstrate the shape.",
        "value": {
          "basicAuth": true,
          "basicAuthUser": "grafana",
          "jsonData": {
            "derivedFields": [
              {
                "datasourceUid": "tempo",
                "matcherRegex": "traceID=(\\w+)",
                "matcherType": "regex",
                "name": "TraceID",
                "targetBlank": false,
                "url": "${__value.raw}"
              },
              {
                "matcherRegex": "req_id=(\\w+)",
                "matcherType": "regex",
                "name": "requestId",
                "targetBlank": true,
                "url": "https://requests.example.com/${__value.raw}",
                "urlDisplayLabel": "View request"
              }
            ],
            "maxLines": "1500"
          },
          "secureJsonData": {
            "basicAuthPassword": "REPLACE_WITH_PASSWORD"
          },
          "url": "https://loki.example.com"
        }
      }
    }
  }
}
