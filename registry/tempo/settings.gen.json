{
  "spec": {
    "type": "object",
    "properties": {
      "basicAuth": {
        "type": "boolean",
        "default": false
      },
      "basicAuthUser": {
        "description": "The username of the data source account",
        "type": "string",
        "x-dsconfig-depends-on": "virtual_authMethod == 'BasicAuth'",
        "x-dsconfig-required-when": "root_basicAuth == true"
      },
      "jsonData": {
        "type": "object",
        "properties": {
          "keepCookies": {
            "description": "Grafana proxy deletes forwarded cookies by default. Specify cookies by name that should be forwarded to the data source.",
            "type": "array",
            "items": {
              "type": "string"
            }
          },
          "nodeGraph": {
            "description": "Show or hide the node graph visualization.",
            "type": "object"
          },
          "oauthPassThru": {
            "type": "boolean",
            "default": false
          },
          "search": {
            "description": "Modify how traces are searched.",
            "type": "object"
          },
          "serverName": {
            "description": "A Servername is used to verify the hostname on the returned certificate",
            "type": "string",
            "x-dsconfig-depends-on": "jsonData_tlsAuth == true",
            "x-dsconfig-required-when": "jsonData_tlsAuth == true"
          },
          "serviceMap": {
            "description": "Select a Prometheus data source that contains the service graph data.",
            "type": "object"
          },
          "spanBar": {
            "description": "Add additional info next to the service and operation on a span bar row in the trace view.",
            "type": "object"
          },
          "streamingEnabled": {
            "description": "Enable streaming for different Tempo features.",
            "type": "object"
          },
          "tagLimit": {
            "description": "Specify the max number of tags and tag values to display in the Tempo editor. Default: 5000",
            "type": "number"
          },
          "timeRangeForTags": {
            "description": "Time range in tags and tag value queries",
            "type": "number",
            "default": 1800,
            "enum": [
              1800,
              10800,
              86400,
              259200,
              604800
            ]
          },
          "timeout": {
            "description": "HTTP request timeout in seconds",
            "type": "number"
          },
          "tlsAuth": {
            "description": "Validate using TLS client authentication, in which the server authenticates the client",
            "type": "boolean",
            "default": false
          },
          "tlsAuthWithCACert": {
            "description": "Add your own Certificate Authority (CA) certificate on top of one generated by the certificate authorities for additional security measures",
            "type": "boolean",
            "default": false
          },
          "tlsSkipVerify": {
            "description": "Skipping TLS certificate validation is not recommended unless absolutely necessary or for testing",
            "type": "boolean",
            "default": false
          },
          "traceQuery": {
            "description": "Modify how TraceID queries are run.",
            "type": "object"
          },
          "tracesToLogs": {
            "description": "Legacy v1 shape for Trace to logs. `getTraceToLogsOptions` (packages/grafana-o11y-ds-frontend/src/TraceToLogs/TraceToLogsSettings.tsx:55-73) migrates this to `tracesToLogsV2` on read; the editor clears this key whenever `tracesToLogsV2` is written. Retained here for round-trip compatibility of legacy datasources.",
            "type": "object"
          },
          "tracesToLogsV2": {
            "description": "Navigate from a trace span to the selected data source's logs.",
            "type": "object"
          },
          "tracesToMetrics": {
            "description": "Navigate from a trace span to the selected data source's metrics.",
            "type": "object"
          },
          "tracesToProfiles": {
            "description": "Navigate from a trace span to the selected data source's profiles.",
            "type": "object"
          }
        }
      },
      "url": {
        "type": "string",
        "x-dsconfig-required-when": "true"
      }
    }
  },
  "secureValues": [
    {
      "key": "basicAuthPassword",
      "description": "The password of the data source account"
    },
    {
      "key": "tlsCACert",
      "description": "Your self-signed certificate"
    },
    {
      "key": "tlsClientCert",
      "description": "The client certificate can be generated from a Certificate Authority or be self-signed"
    },
    {
      "key": "tlsClientKey",
      "description": "The client key can be generated from a Certificate Authority or be self-signed"
    }
  ]
}
