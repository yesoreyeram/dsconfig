{
    "$schema": "https://raw.githubusercontent.com/grafana/dsconfig/refs/heads/main/dsconfig/schema.json",
    "schemaVersion": "v1",
    "pluginType": "tempo",
    "pluginName": "Tempo",
    "docURL": "https://grafana.com/docs/grafana/latest/datasources/tempo/",
    "fields": [
        {
            "id": "root_url",
            "key": "url",
            "label": "URL",
            "valueType": "string",
            "target": "root",
            "role": "endpoint.baseUrl",
            "requiredWhen": "true",
            "ui": {
                "component": "input",
                "placeholder": "http://localhost:3200"
            }
        },
        {
            "id": "virtual_authMethod",
            "key": "authMethod",
            "label": "Authentication method",
            "description": "Choose an authentication method to access the data source",
            "valueType": "string",
            "kind": "virtual",
            "defaultValue": "NoAuth",
            "validations": [
                {
                    "type": "allowedValues",
                    "values": [
                        "BasicAuth",
                        "OAuthForward",
                        "NoAuth"
                    ]
                }
            ],
            "ui": {
                "component": "select",
                "options": [
                    {
                        "label": "Basic authentication",
                        "value": "BasicAuth"
                    },
                    {
                        "label": "Forward OAuth Identity",
                        "value": "OAuthForward"
                    },
                    {
                        "label": "No Authentication",
                        "value": "NoAuth"
                    }
                ]
            },
            "storage": {
                "type": "computed",
                "read": "root.basicAuth == true ? 'BasicAuth' : (jsonData.oauthPassThru == true ? 'OAuthForward' : 'NoAuth')"
            },
            "effects": [
                {
                    "when": "value == 'BasicAuth'",
                    "set": {
                        "root_basicAuth": true,
                        "jsonData_oauthPassThru": false
                    }
                },
                {
                    "when": "value == 'OAuthForward'",
                    "set": {
                        "root_basicAuth": false,
                        "jsonData_oauthPassThru": true
                    }
                },
                {
                    "when": "value == 'NoAuth'",
                    "set": {
                        "root_basicAuth": false,
                        "jsonData_oauthPassThru": false
                    }
                }
            ]
        },
        {
            "id": "root_basicAuth",
            "key": "basicAuth",
            "valueType": "boolean",
            "target": "root",
            "role": "auth.basic.enabled",
            "defaultValue": false,
            "tags": [
                "managed-by:virtual_authMethod"
            ]
        },
        {
            "id": "root_basicAuthUser",
            "key": "basicAuthUser",
            "label": "User",
            "description": "The username of the data source account",
            "valueType": "string",
            "target": "root",
            "role": "auth.basic.username",
            "dependsOn": "virtual_authMethod == 'BasicAuth'",
            "requiredWhen": "root_basicAuth == true",
            "ui": {
                "component": "input",
                "placeholder": "User"
            }
        },
        {
            "id": "secureJsonData_basicAuthPassword",
            "key": "basicAuthPassword",
            "label": "Password",
            "description": "The password of the data source account",
            "valueType": "string",
            "target": "secureJsonData",
            "role": "auth.basic.password",
            "dependsOn": "virtual_authMethod == 'BasicAuth'",
            "requiredWhen": "root_basicAuth == true",
            "ui": {
                "component": "input",
                "placeholder": "Password"
            }
        },
        {
            "id": "jsonData_oauthPassThru",
            "key": "oauthPassThru",
            "valueType": "boolean",
            "target": "jsonData",
            "role": "auth.forwardOAuthToken.enabled",
            "defaultValue": false,
            "tags": [
                "managed-by:virtual_authMethod"
            ]
        },
        {
            "id": "jsonData_tlsAuthWithCACert",
            "key": "tlsAuthWithCACert",
            "label": "Add self-signed certificate",
            "description": "Add your own Certificate Authority (CA) certificate on top of one generated by the certificate authorities for additional security measures",
            "valueType": "boolean",
            "target": "jsonData",
            "defaultValue": false,
            "ui": {
                "component": "switch"
            }
        },
        {
            "id": "secureJsonData_tlsCACert",
            "key": "tlsCACert",
            "label": "CA Certificate",
            "description": "Your self-signed certificate",
            "valueType": "string",
            "target": "secureJsonData",
            "role": "tls.caCert",
            "dependsOn": "jsonData_tlsAuthWithCACert == true",
            "requiredWhen": "jsonData_tlsAuthWithCACert == true",
            "ui": {
                "component": "textarea",
                "multiline": true,
                "rows": 6,
                "placeholder": "Begins with --- BEGIN CERTIFICATE ---"
            }
        },
        {
            "id": "jsonData_tlsAuth",
            "key": "tlsAuth",
            "label": "TLS Client Authentication",
            "description": "Validate using TLS client authentication, in which the server authenticates the client",
            "valueType": "boolean",
            "target": "jsonData",
            "defaultValue": false,
            "ui": {
                "component": "switch"
            }
        },
        {
            "id": "jsonData_serverName",
            "key": "serverName",
            "label": "ServerName",
            "description": "A Servername is used to verify the hostname on the returned certificate",
            "valueType": "string",
            "target": "jsonData",
            "role": "tls.serverName",
            "dependsOn": "jsonData_tlsAuth == true",
            "requiredWhen": "jsonData_tlsAuth == true",
            "ui": {
                "component": "input",
                "placeholder": "domain.example.com"
            }
        },
        {
            "id": "secureJsonData_tlsClientCert",
            "key": "tlsClientCert",
            "label": "Client Certificate",
            "description": "The client certificate can be generated from a Certificate Authority or be self-signed",
            "valueType": "string",
            "target": "secureJsonData",
            "role": "tls.clientCert",
            "dependsOn": "jsonData_tlsAuth == true",
            "requiredWhen": "jsonData_tlsAuth == true",
            "ui": {
                "component": "textarea",
                "multiline": true,
                "rows": 6,
                "placeholder": "Begins with --- BEGIN CERTIFICATE ---"
            }
        },
        {
            "id": "secureJsonData_tlsClientKey",
            "key": "tlsClientKey",
            "label": "Client Key",
            "description": "The client key can be generated from a Certificate Authority or be self-signed",
            "valueType": "string",
            "target": "secureJsonData",
            "role": "tls.clientKey",
            "dependsOn": "jsonData_tlsAuth == true",
            "requiredWhen": "jsonData_tlsAuth == true",
            "ui": {
                "component": "textarea",
                "multiline": true,
                "rows": 6,
                "placeholder": "Begins with --- RSA PRIVATE KEY CERTIFICATE ---"
            }
        },
        {
            "id": "jsonData_tlsSkipVerify",
            "key": "tlsSkipVerify",
            "label": "Skip TLS certificate validation",
            "description": "Skipping TLS certificate validation is not recommended unless absolutely necessary or for testing",
            "valueType": "boolean",
            "target": "jsonData",
            "role": "transport.tlsSkipVerify",
            "defaultValue": false,
            "ui": {
                "component": "switch"
            }
        },
        {
            "id": "jsonData_keepCookies",
            "key": "keepCookies",
            "label": "Allowed cookies",
            "description": "Grafana proxy deletes forwarded cookies by default. Specify cookies by name that should be forwarded to the data source.",
            "valueType": "array",
            "target": "jsonData",
            "item": {
                "valueType": "string"
            },
            "ui": {
                "component": "list",
                "placeholder": "New cookie (hit enter to add)"
            }
        },
        {
            "id": "jsonData_timeout",
            "key": "timeout",
            "label": "Timeout",
            "description": "HTTP request timeout in seconds",
            "valueType": "number",
            "target": "jsonData",
            "role": "transport.timeoutSeconds",
            "ui": {
                "component": "input",
                "placeholder": "Timeout in seconds"
            }
        },
        {
            "id": "jsonData_streamingEnabled",
            "key": "streamingEnabled",
            "label": "Streaming",
            "description": "Enable streaming for different Tempo features.",
            "valueType": "object",
            "target": "jsonData",
            "help": {
                "markdown": "Enable streaming for different Tempo features. If your Tempo instance is behind a load balancer or proxy that does not support gRPC or HTTP2, streaming will probably not work and should be disabled.\n\nShape: `{ search?: boolean, metrics?: boolean }`.\n\n- `search` enables streaming for TraceQL search queries. Minimum required Tempo version: **2.2.0**.\n- `metrics` enables streaming for TraceQL metrics queries. Minimum required Tempo version: **2.7.0**.\n\nSee [Stream query results](https://grafana.com/docs/tempo/latest/traceql/#stream-query-results) in the Tempo docs."
            }
        },
        {
            "id": "jsonData_tracesToLogsV2",
            "key": "tracesToLogsV2",
            "label": "Trace to logs",
            "description": "Navigate from a trace span to the selected data source's logs.",
            "valueType": "object",
            "target": "jsonData",
            "help": {
                "markdown": "Navigate from a trace span to the selected logs data source. Written by `@grafana/o11y-ds-frontend`'s `TraceToLogsSection` (v2 shape); the editor clears the legacy `tracesToLogs` key on any write.\n\nShape: `{ datasourceUid?: string, tags?: Array<{ key: string, value?: string }>, spanStartTimeShift?: string, spanEndTimeShift?: string, filterByTraceID?: boolean, filterBySpanID?: boolean, query?: string, customQuery: boolean }`.\n\n- `datasourceUid` — target logs datasource UID (loki, elasticsearch, splunk, opensearch, falconlogscale, googlecloud-logging, victoriametrics-logs).\n- `tags` — key/value tag mappings used to build the log query (default tags: `cluster`, `hostname`, `namespace`, `pod`, `service.name`, `service.namespace`).\n- `spanStartTimeShift` / `spanEndTimeShift` — time-range shifts applied when jumping to logs (e.g. `-1m`, `2m`).\n- `filterByTraceID` / `filterBySpanID` — filter the logs by the trace/span ID; disabled when `customQuery` is on.\n- `customQuery` — use a custom query template; when true, `query` is used with `$__tags` interpolation.\n\nDocs: [Trace to logs](https://grafana.com/docs/grafana/latest/datasources/tempo/configure-tempo-data-source/#trace-to-logs)."
            }
        },
        {
            "id": "jsonData_tracesToLogs",
            "key": "tracesToLogs",
            "valueType": "object",
            "target": "jsonData",
            "description": "Legacy v1 shape for Trace to logs. `getTraceToLogsOptions` (packages/grafana-o11y-ds-frontend/src/TraceToLogs/TraceToLogsSettings.tsx:55-73) migrates this to `tracesToLogsV2` on read; the editor clears this key whenever `tracesToLogsV2` is written. Retained here for round-trip compatibility of legacy datasources.",
            "tags": [
                "legacy"
            ]
        },
        {
            "id": "jsonData_tracesToMetrics",
            "key": "tracesToMetrics",
            "label": "Trace to metrics",
            "description": "Navigate from a trace span to the selected data source's metrics.",
            "valueType": "object",
            "target": "jsonData",
            "help": {
                "markdown": "Navigate from a trace span to the selected metrics data source (Prometheus or victoriametrics-metrics). Written by `@grafana/o11y-ds-frontend`'s `TraceToMetricsSection`.\n\nShape: `{ datasourceUid?: string, tags?: Array<{ key: string, value: string }>, queries: Array<{ name?: string, query?: string }>, spanStartTimeShift?: string, spanEndTimeShift?: string }`.\n\n- `datasourceUid` — target metrics datasource UID.\n- `tags` — key/value tag mappings used in the metrics query.\n- `queries` — list of Prometheus queries offered on the trace→metrics link, each with a display name and a query template that interpolates `$__tags`.\n- `spanStartTimeShift` / `spanEndTimeShift` — time-range shifts (default placeholders `-2m` / `2m`).\n\nDocs: [Trace to metrics](https://grafana.com/docs/grafana/latest/datasources/tempo/configure-tempo-data-source/#trace-to-metrics)."
            }
        },
        {
            "id": "jsonData_tracesToProfiles",
            "key": "tracesToProfiles",
            "label": "Trace to profiles",
            "description": "Navigate from a trace span to the selected data source's profiles.",
            "valueType": "object",
            "target": "jsonData",
            "help": {
                "markdown": "Navigate from a trace span to the selected Pyroscope profiles data source. Written by `@grafana/o11y-ds-frontend`'s `TraceToProfilesSection`.\n\nShape: `{ datasourceUid?: string, tags?: Array<{ key: string, value?: string }>, profileTypeId?: string, query?: string, customQuery: boolean }`.\n\n- `datasourceUid` — target Pyroscope datasource UID (`grafana-pyroscope-datasource`).\n- `tags` — key/value tag mappings used in the profile query (default tags: `service.name`, `service.namespace`).\n- `profileTypeId` — Pyroscope profile type identifier, populated from the target datasource's `profileTypes` resource.\n- `customQuery` — use a custom query template; when true, `query` is used with `$__tags` interpolation.\n\nDocs: [Trace to profiles](https://grafana.com/docs/grafana/latest/datasources/tempo/configure-tempo-data-source/#trace-to-profiles)."
            }
        },
        {
            "id": "jsonData_serviceMap",
            "key": "serviceMap",
            "label": "Service graph",
            "description": "Select a Prometheus data source that contains the service graph data.",
            "valueType": "object",
            "target": "jsonData",
            "help": {
                "markdown": "Select the Prometheus data source that contains the service graph metrics (`traces_service_graph_*`) generated by Tempo's `service_graphs` processor.\n\nShape: `{ datasourceUid?: string }`.\n\nDocs: [Service graph](https://grafana.com/docs/grafana/latest/datasources/tempo/configure-tempo-data-source/#service-graph)."
            }
        },
        {
            "id": "jsonData_nodeGraph",
            "key": "nodeGraph",
            "label": "Node graph",
            "description": "Show or hide the node graph visualization.",
            "valueType": "object",
            "target": "jsonData",
            "help": {
                "markdown": "Display the node graph above the trace view. Written by `@grafana/o11y-ds-frontend`'s `NodeGraphSection`.\n\nShape: `{ enabled?: boolean }`. Default: disabled.\n\nDocs: [Node graph](https://grafana.com/docs/grafana/latest/datasources/tempo/configure-tempo-data-source/#node-graph)."
            }
        },
        {
            "id": "jsonData_search",
            "key": "search",
            "label": "Tempo search",
            "description": "Modify how traces are searched.",
            "valueType": "object",
            "target": "jsonData",
            "help": {
                "markdown": "Configure the TraceQL search tab in the query editor.\n\nShape: `{ hide?: boolean, filters?: TraceqlFilter[] }`.\n\n- `hide` — remove the search tab from the query editor (tooltip: \"Removes the search tab from the query editor\").\n- `filters` — pre-configured static filters exposed in the UI. Each filter is `{ id: string, tag?: string, operator?: string, scope?: 'event'|'instrumentation'|'intrinsic'|'link'|'resource'|'span'|'unscoped', value?: string | string[], valueType?: string, isCustomValue?: boolean }`.\n\nOn first load the frontend seeds `filters` with two default entries (`service.name` and `span.name` in the `resource`/`span` scopes) — see `src/datasource.ts:147-159`.\n\nDocs: [Tempo search](https://grafana.com/docs/grafana/latest/datasources/tempo/configure-tempo-data-source/#tempo-search)."
            }
        },
        {
            "id": "jsonData_traceQuery",
            "key": "traceQuery",
            "label": "TraceID query",
            "description": "Modify how TraceID queries are run.",
            "valueType": "object",
            "target": "jsonData",
            "help": {
                "markdown": "Control the time range applied when running a query by TraceID.\n\nShape: `{ timeShiftEnabled?: boolean, spanStartTimeShift?: string, spanEndTimeShift?: string }`.\n\n- `timeShiftEnabled` — \"Use time range in query\" toggle (tooltip: \"The time range can be used when there are performance issues or timeouts since it will narrow down the search to the defined range. Default: disabled\"). Default: `false`.\n- `spanStartTimeShift` — \"Time shift for start of search\" (tooltip: \"Shifts the start of the time range when searching by TraceID. Searching can return traces that do not fully fall into the search time range, so we recommend using higher time shifts for longer traces. Default: 30m (Time units can be used here, for example: 5s, 1m, 3h\").\n- `spanEndTimeShift` — \"Time shift for end of search\" (same rule for the end of the range).\n\nInputs are disabled unless `timeShiftEnabled` is `true`.\n\nDocs: [TraceID query](https://grafana.com/docs/grafana/latest/datasources/tempo/configure-tempo-data-source/#traceid-query)."
            }
        },
        {
            "id": "jsonData_timeRangeForTags",
            "key": "timeRangeForTags",
            "label": "Time range in query",
            "description": "Time range in tags and tag value queries",
            "valueType": "number",
            "target": "jsonData",
            "defaultValue": 1800,
            "validations": [
                {
                    "type": "allowedValues",
                    "values": [
                        1800,
                        10800,
                        86400,
                        259200,
                        604800
                    ]
                }
            ],
            "ui": {
                "component": "select",
                "placeholder": "Time range for tags",
                "options": [
                    {
                        "label": "Last 30 minutes of selected range",
                        "value": 1800
                    },
                    {
                        "label": "Last 3 hours of selected range",
                        "value": 10800
                    },
                    {
                        "label": "Last 24 hours of selected range",
                        "value": 86400
                    },
                    {
                        "label": "Last 3 days of selected range",
                        "value": 259200
                    },
                    {
                        "label": "Last 7 days of selected range",
                        "value": 604800
                    }
                ]
            }
        },
        {
            "id": "jsonData_tagLimit",
            "key": "tagLimit",
            "label": "Max tags and tag values",
            "description": "Specify the max number of tags and tag values to display in the Tempo editor. Default: 5000",
            "valueType": "number",
            "target": "jsonData",
            "ui": {
                "component": "input",
                "placeholder": "5000"
            }
        },
        {
            "id": "jsonData_spanBar",
            "key": "spanBar",
            "label": "Span bar",
            "description": "Add additional info next to the service and operation on a span bar row in the trace view.",
            "valueType": "object",
            "target": "jsonData",
            "help": {
                "markdown": "Additional label rendered next to service/operation on each span bar row in the trace view. Written by `@grafana/o11y-ds-frontend`'s `SpanBarSection`.\n\nShape: `{ type?: 'None' | 'Duration' | 'Tag', tag?: string }`.\n\n- `type` — label source. `'None'` hides the extra label; `'Duration'` (default, shown as placeholder in the editor) renders the span duration; `'Tag'` renders the value of the tag named by `tag`.\n- `tag` — required when `type` is `'Tag'`; the tag key whose value is displayed (searched in span attributes and resource attributes).\n\nDocs: [Span bar](https://grafana.com/docs/grafana/latest/datasources/tempo/configure-tempo-data-source/#span-bar)."
            }
        }
    ],
    "groups": [
        {
            "id": "connection",
            "title": "Connection",
            "order": 1,
            "fieldRefs": [
                "root_url"
            ]
        },
        {
            "id": "authentication",
            "title": "Authentication",
            "order": 2,
            "fieldRefs": [
                "virtual_authMethod",
                "root_basicAuthUser",
                "secureJsonData_basicAuthPassword"
            ]
        },
        {
            "id": "tls-settings",
            "title": "TLS settings",
            "order": 3,
            "optional": true,
            "fieldRefs": [
                "jsonData_tlsAuthWithCACert",
                "secureJsonData_tlsCACert",
                "jsonData_tlsAuth",
                "jsonData_serverName",
                "secureJsonData_tlsClientCert",
                "secureJsonData_tlsClientKey",
                "jsonData_tlsSkipVerify"
            ]
        },
        {
            "id": "advanced-http",
            "title": "Advanced HTTP settings",
            "order": 4,
            "optional": true,
            "fieldRefs": [
                "jsonData_keepCookies",
                "jsonData_timeout"
            ]
        },
        {
            "id": "streaming",
            "title": "Streaming",
            "order": 5,
            "fieldRefs": [
                "jsonData_streamingEnabled"
            ]
        },
        {
            "id": "trace-to-logs",
            "title": "Trace to logs",
            "order": 6,
            "optional": true,
            "fieldRefs": [
                "jsonData_tracesToLogsV2",
                "jsonData_tracesToLogs"
            ]
        },
        {
            "id": "trace-to-metrics",
            "title": "Trace to metrics",
            "order": 7,
            "optional": true,
            "fieldRefs": [
                "jsonData_tracesToMetrics"
            ]
        },
        {
            "id": "trace-to-profiles",
            "title": "Trace to profiles",
            "order": 8,
            "optional": true,
            "fieldRefs": [
                "jsonData_tracesToProfiles"
            ]
        },
        {
            "id": "service-graph",
            "title": "Service graph",
            "order": 9,
            "optional": true,
            "fieldRefs": [
                "jsonData_serviceMap"
            ]
        },
        {
            "id": "node-graph",
            "title": "Node graph",
            "order": 10,
            "optional": true,
            "fieldRefs": [
                "jsonData_nodeGraph"
            ]
        },
        {
            "id": "tempo-search",
            "title": "Tempo search",
            "order": 11,
            "optional": true,
            "fieldRefs": [
                "jsonData_search"
            ]
        },
        {
            "id": "traceid-query",
            "title": "TraceID query",
            "order": 12,
            "optional": true,
            "fieldRefs": [
                "jsonData_traceQuery"
            ]
        },
        {
            "id": "tags-time-range",
            "title": "Tags time range",
            "order": 13,
            "optional": true,
            "fieldRefs": [
                "jsonData_timeRangeForTags"
            ]
        },
        {
            "id": "tag-limit",
            "title": "Tag limit",
            "order": 14,
            "optional": true,
            "fieldRefs": [
                "jsonData_tagLimit"
            ]
        },
        {
            "id": "span-bar",
            "title": "Span bar",
            "order": 15,
            "optional": true,
            "fieldRefs": [
                "jsonData_spanBar"
            ]
        }
    ],
    "relationships": [
        {
            "type": "pair",
            "fields": [
                "root_basicAuthUser",
                "secureJsonData_basicAuthPassword"
            ],
            "description": "Basic auth credentials. Both fields are used together when root.basicAuth is true."
        },
        {
            "type": "group",
            "fields": [
                "jsonData_tlsAuth",
                "jsonData_serverName",
                "secureJsonData_tlsClientCert",
                "secureJsonData_tlsClientKey"
            ],
            "description": "TLS mutual auth: enabling jsonData.tlsAuth requires serverName + client cert + client key. See @grafana/plugin-ui TLSClientAuth.tsx."
        },
        {
            "type": "pair",
            "fields": [
                "jsonData_tlsAuthWithCACert",
                "secureJsonData_tlsCACert"
            ],
            "description": "Self-signed / custom CA verification: enabling jsonData.tlsAuthWithCACert requires the CA certificate PEM in secureJsonData.tlsCACert."
        },
        {
            "type": "group",
            "fields": [
                "jsonData_tracesToLogsV2",
                "jsonData_tracesToLogs"
            ],
            "description": "tracesToLogs (v1) is legacy; the editor migrates it to tracesToLogsV2 on read (getTraceToLogsOptions) and clears it on any write to tracesToLogsV2. Provisioning payloads should prefer tracesToLogsV2."
        }
    ],
    "instructions": [
        {
            "msg": "Tempo authentication is built on Grafana's HTTP settings model, not a discriminator field. The editor's three visible auth methods write these flags: 'Basic authentication' sets root.basicAuth=true and provides root.basicAuthUser + secureJsonData.basicAuthPassword; 'Forward OAuth Identity' sets jsonData.oauthPassThru=true; 'No Authentication' leaves both false. Only one of basicAuth / oauthPassThru should be true at a time — the editor's onAuthMethodSelect (@grafana/plugin-ui utils.ts:44-54) always writes both. The Tempo backend uses BasicAuthEnabled + settings.HTTPClientOptions for HTTP requests (pkg/tempo/tempo.go:54-72) and gRPC streaming (pkg/tempo/grpc.go:178-184) — Basic auth applies to both transports.",
            "tags": [
                "llm",
                "auth"
            ]
        },
        {
            "msg": "Minimal Basic-auth payload: {\"url\":\"http://localhost:3200\",\"basicAuth\":true,\"basicAuthUser\":\"<user>\",\"jsonData\":{},\"secureJsonData\":{\"basicAuthPassword\":\"<password>\"}}. Minimal No-auth payload: {\"url\":\"http://localhost:3200\",\"jsonData\":{}}. Minimal OAuth-forward payload: {\"url\":\"http://localhost:3200\",\"jsonData\":{\"oauthPassThru\":true}}. root.url is required — the Tempo backend reads settings.URL directly (pkg/tempo/tempo.go:78, pkg/tempo/grpc.go:86) and issues HTTP requests against '<url>/api/echo' (health check, tempo.go:219) and '<url>/api/v2/search/tags' (resource proxy, tempo.go:257). The URL is also parsed by the gRPC client (grpc.go:86-100), which appends :80 or :443 if no port is present.",
            "tags": [
                "llm",
                "auth"
            ]
        },
        {
            "msg": "Streaming is opt-in per feature and requires specific minimum Tempo versions. jsonData.streamingEnabled.search=true enables TraceQL search streaming (min Tempo 2.2.0); jsonData.streamingEnabled.metrics=true enables TraceQL metrics streaming (min Tempo 2.7.0). When streamingEnabled.search is true, CheckHealth (tempo.go:150-208) probes the gRPC streaming endpoint instead of the HTTP /api/echo. Streaming needs end-to-end gRPC/HTTP2: an intermediate load balancer or proxy without gRPC/HTTP2 support will break it, and the editor shows an info alert to that effect (StreamingSection.tsx:35-38).",
            "tags": [
                "llm",
                "streaming"
            ]
        },
        {
            "msg": "Trace-to-X mappings live under three jsonData keys: tracesToLogsV2 (v2 shape written by @grafana/o11y-ds-frontend's TraceToLogsSection — the editor clears the legacy tracesToLogs key on any write; getTraceToLogsOptions migrates old→new on read), tracesToMetrics (single shape, may contain multiple named queries), and tracesToProfiles (single shape, targets grafana-pyroscope-datasource). Each carries a datasourceUid targeting a specific plugin type: tracesToLogsV2 → loki / elasticsearch / grafana-splunk-datasource / grafana-opensearch-datasource / grafana-falconlogscale-datasource / googlecloud-logging-datasource / victoriametrics-logs-datasource; tracesToMetrics → prometheus / victoriametrics-metrics-datasource; tracesToProfiles → grafana-pyroscope-datasource. Provisioning payloads should write tracesToLogsV2 directly and omit tracesToLogs.",
            "tags": [
                "llm",
                "traceLinking"
            ]
        },
        {
            "msg": "secureJsonData values (basicAuthPassword, tlsCACert, tlsClientCert, tlsClientKey) are write-only: they can be set or reset but never read back. To check whether a secret is already configured on an existing datasource, read the secureJsonFields object (e.g. secureJsonFields.basicAuthPassword == true) instead of secureJsonData. Custom HTTP header values follow the same pattern via httpHeaderValue<N> secrets (see the entry README) — the Tempo backend forwards them on both HTTP and gRPC streaming calls (grpc.go:234-246).",
            "tags": [
                "llm",
                "auth",
                "secure"
            ]
        },
        {
            "msg": "Tempo-only knobs that shape the query editor rather than the backend: jsonData.tagLimit caps the max tags/values fetched by the editor (default 5000, stored as either number or numeric string — the editor writes v.currentTarget.value from Input type=number, TagLimitSettings.tsx:32); jsonData.timeRangeForTags picks one of five fixed windows in seconds (1800 / 10800 / 86400 / 259200 / 604800, default 1800); jsonData.search.hide removes the TraceQL search tab; jsonData.search.filters seeds static filters; jsonData.traceQuery.timeShiftEnabled + spanStartTimeShift + spanEndTimeShift narrow TraceID query time windows; jsonData.spanBar.type ('None'|'Duration'|'Tag') + jsonData.spanBar.tag decorate span rows; jsonData.nodeGraph.enabled toggles the node graph; jsonData.serviceMap.datasourceUid points at the Prometheus datasource that stores the service graph metrics. None of these are read by the Tempo Go backend — they are consumed by the frontend datasource class and the trace view.",
            "tags": [
                "llm",
                "settings"
            ]
        }
    ]
}
